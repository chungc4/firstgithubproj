javascript var:
console.log("Hello")
document.write("Hello");
	string
	number
	boolean
		if else; for loop
	array
		var scores = [ 80, 79, 88, 78, 100]
		scores [0] /*first entry*/
	object
	+
	-
	*
	/
	%
	== equals to
	!== not equals to
	++ x+1

/*   From  Until  Final condition
for (i=0; i<10;;i++){
	console.log(i)
}

mkdir = make directory
rmdir = remove directory
cd = change directory
touch = create index.html?

git--version
git init
*git add .
*git commit-m

git remote add origin <github url>
*git push origina master